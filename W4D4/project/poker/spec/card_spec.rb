require "card"

describe Card do
    describe "#initialize" do
        subject(:new_card){Card.new(:heart, 1, :ONE)}
        # it "instantiate" do
        #     expect(subject).to eq(subject)
        # end
    
    end
end